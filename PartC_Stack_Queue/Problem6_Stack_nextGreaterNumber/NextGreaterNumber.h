#pragma once 
#include<stack>
#include<vector>
using namespace std; 

class NextGreaterNumber {
private : 
	vector<int>nums; 
	stack<int>st; 

public:
	NextGreaterNumber(vector<int>nums)
	{
		this->nums = nums; 
	}

	vector<int>solve()
	{
		vector<int>result(-1, nums.size()); 
		for(int i = 0 ; i < )
	}
};